/*escribí una función sumarMonedasDe25(cantidadDeMonedas), que tome como parámetro un valor 
numérico y retorne la sumatoria de las monedas de 25 centavos.*/





/*function sumarMonedasDe25(cantidadDeMonedas){

    let suma= 0

    for(let i = 0 ; i < cantidadDeMonedas ; i++ ){

        suma =suma + 0.25;
     }
     return(suma);
}

console.log(sumarMonedasDe25(1));


*/






/*Definí ahora la función sumaDeLosParesDel0Al(x) que tome como parámetro un número X y 
que devuelva el total de la suma de todos los números que son pares del 0 al X (incluyendo a X). 
Suponiendo que X es par ésta función hará la siguiente operación: 0 + 2 + 4 + 6 + 8 + 10 + ... x*/

function sumaDeLosParesDel0Al(x) {

    let suma = 0;
    

    for (i = 0; i < sumaDeLosParesDel0Al; i++) {


        if (i % 2 == 0) {

            suma = suma + sumaDeLosParesDel0Al




        }

    }
    return (suma);
}
sumaDeLosParesDel0Al(10);